@model IEnumerable<SocialNetwork1._1.Models.ApplicationUser>

@{
    ViewBag.Title = "UserFriends";
}

<div class="row">
    <div class="col-md-9">
        @if (Model != null)
        {
            if (Model.Count() == 0)
            {
                <h4>You have not friends :(</h4>
            }
            else
            {
                @Html.Partial("~/Views/Partial/UserList.cshtml")
            }
        }
    </div>
</div>

